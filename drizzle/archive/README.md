# Archived migrations

`0002_new_modules.sql` was an unjournaled duplicate of the active `0002_cute_the_hood.sql` migration. It is preserved here for historical reference but intentionally kept outside the active Drizzle migration directory. The active sequence is defined by `drizzle/meta/_journal.json`; future schema changes must use the next sequential migration identifier and update the journal through the normal Drizzle workflow.
